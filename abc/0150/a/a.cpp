#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<(n);i++)

int main(int argc, char const *argv[])
{
	int k, x; cin >> k >> x;
	if (k * 500 >= x) {
		printf("Yes\n");
	} else {
		printf("No\n");
	}
	return 0;
}
