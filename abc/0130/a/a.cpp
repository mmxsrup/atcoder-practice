#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i,n) for(int i=0;i<(n);i++)

int main(int argc, char const *argv[]) {
	int x, a; cin >> x >> a;
	if (x < a) printf("%d\n", 0);
	else printf("%d\n", 10);
	return 0;
}
